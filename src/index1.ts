// Ejercicio Sueldos
/*let sueldo = Number(prompt(`Ingrese Sueldo`));
let aumentoSueldo : number;

switch (true) {
  case sueldo > 0 && sueldo <= 15000:
    aumentoSueldo = sueldo * 0.2;
    console.log(`El aumento de su Sueldo es del 20%, + ${aumentoSueldo} pesos`);
   break;
  case sueldo >= 15001 && sueldo <= 20000:
    aumentoSueldo = sueldo * 0.1;
    console.log(`El aumento de su Sueldo es del 10%, + ${aumentoSueldo} pesos`);
   break;
  case sueldo >= 20001 && sueldo <= 25000:
    aumentoSueldo = sueldo * 0.05;
    console.log(`El aumento de su Sueldo es del 5%, + ${aumentoSueldo} pesos`);
  default;
    case sueldo > 25000:
    console.log("Su sueldo no tiene aumento");
    break;
}*/
